const { EmbedBuilder, Message } = require("discord.js")

module.exports = {
    name: "",
    aliases: [""],

    run: async(client, msg, args) => {
        /**
         * @type {Message}
         */
        let message = msg

        
    }
}